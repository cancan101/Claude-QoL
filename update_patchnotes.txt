Add "Copy as rich text" button to content blocks — pastes with formatting into email, Docs, etc.
Add timestamp/model slug in HTML/MD exports
Store active flags in storage until expiry so they don't disappear early
